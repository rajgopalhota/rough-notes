<template for:each={resultData.questions} for:item="q" for:index="index">
    <div key={q.id} class="slds-m-bottom_medium">

        <!-- Label -->
        <label 
            class="slds-form-element__label bold-label" 
            for={q.id}
            id={q.id + '-label'}
        >
            {q.text}
            <span style="color: red;">*</span>
        </label>

        <!-- Input -->
        <lightning-input 
            id={q.id}
            name={q.id}
            type={q.type}
            date-style="short"
            required
            maxlength={q.maxlength}
            onchange={handleValidations}
            variant="label-hidden"
            class={q.dob}
        >
        </lightning-input>

        <!-- Checkbox List -->
        <template for:each={q.autoFillCheckboxList} for:item="eachCheckBox">
            <div key={eachCheckBox.id} class={eachCheckBox.className}>
                
                <lightning-input 
                    type="checkbox"
                    label={eachCheckBox.label}
                    class={eachCheckBox.inputClassName}
                    onchange={handleCheckBoxOnChange}
                >
                </lightning-input>

            </div>
        </template>

    </div>
</template>